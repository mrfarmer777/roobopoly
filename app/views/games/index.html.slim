h1 All the Games!
table
  thead
    tr
      th Game
      th Players
  tbody
    - @games.each do |game|
      tr.game
        td = link_to "Go to game", game_path(game)
        td
          = game.players.map(&:name).join(', ')
= link_to 'Create a New Game', new_game_path
